# This file contains metadata for your plugin.

# This file should be included when you package your plugin.# Mandatory items:

[general]
name=RAIES
qgisMinimumVersion=3.40
qgisMaximumVersion=3.99
description= Repulsion/Attraction/Inclusion/Exclusion/Sanctuarization Model
version=2.0
author=Vincent Majorczyk
email=vincent.majo@gmail.com

about= RAIES defines constraints on vector layers and combines them into a cumulated raster that highlights the best locations for a project (for example a sediment reuse site depending on habitation proximity and environmental constraints). Constraint types: Attractive, Repulsive, Included, Excluded, Forbidden, Mandatory.

tracker=https://github.com/SURICATES-RAIES/QGIS-RAIES/issues
repository=https://github.com/SURICATES-RAIES/QGIS-RAIES
# End of mandatory metadata

# Recommended items:

hasProcessingProvider=no

changelog=
    2.0 (qgis-3.44)
    - validated on QGIS 3.44 (no code change required vs 3.40)
    - Map layer widget and rasterisation resolution (10/100/1000 m)
    - new Mandatory constraint type (forces the result to 0; Forbidden has priority over Mandatory)
    - Sanctuarized shown as "Forbidden"; Undefined ("?") uses a neutral middle value
    - inline constraint list (replacing the previous tree/groupbox)

# Tags are comma separated with spaces allowed
tags=python,raster,vector,constraints,site selection,sediment

homepage=https://github.com/SURICATES-RAIES/QGIS-RAIES
category=Plugins
icon=icon.png
# experimental flag
experimental=False

# deprecated flag (applies to the whole plugin, not just a single version)
deprecated=False

# Since QGIS 3.8, a comma separated list of plugins to be installed
# (or upgraded) can be specified.
# Check the documentation for more information.
# plugin_dependencies=

Category of the plugin: Raster, Vector, Database or Web
# category=Raster

# If the plugin can run on QGIS Server.
server=False
